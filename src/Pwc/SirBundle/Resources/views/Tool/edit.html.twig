{% extends 'PwcSirBundle:Division:left-small-right-large.html.twig' %}
{% form_theme edit_form 'PwcSirBundle:Form:fields.html.twig' %}

{% block sidebar %}
    {{ knp_menu_render('PwcSirBundle:Builder:settingsMenu', {'currentClass': 'active', 'template': 'PwcSirBundle:Menu:knp_menu.html.twig'}) }}
{% endblock sidebar %}

{% block contentBody %}
<div class="well">
    <form action="{{ path('tool_update', { 'slug': entity.slug }) }}" method="post" {{ form_enctype(edit_form) }} novalidate>
        {{ form_widget(edit_form) }}

        <div class="record_actions">
            <button type="submit" class="btn btn-info">{{ 'form.general.button.save'|trans }}</button>
            <a class="pull-right cancel" href="{{ path('tool') }}">{{ 'form.general.back'|trans }}</a>
        </div>

    </form>
</div>
{% endblock contentBody %}