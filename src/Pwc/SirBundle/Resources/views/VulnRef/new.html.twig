<h1>VulnRef creation</h1>

<form action="{{ path('vulnref_create') }}" method="post" {{ form_enctype(form) }}>
    {{ form_widget(form) }}
    <p>
        <button type="submit">Create</button>
    </p>
</form>

<ul class="record_actions">
    <li>
        <a href="{{ path('vulnref') }}">
            Back to the list
        </a>
    </li>
</ul>
